import { SearchBar } from "./SearchBar";
export default function Home() {
  return (
    <div>
      <SearchBar />
      {/* {allVideos.items.map((video) => {return (
                <h1>{video.etag}</h1>
            )})} */}
    </div>
  );
}
