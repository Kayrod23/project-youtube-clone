export default function Home({allVideos}) {
    console.log(allVideos);
    return (
        <div>
            {/* {allVideos.items.map((video) => {return (
                <h1>{video.etag}</h1>
            )})} */}
        </div>
    )
}